	
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
版本历史
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
基线版本	1, 0, 0, 1	2007.1.1
-----------------------------------------------------------------
版本		1, 0, 0, 2
变更说明	重构了CallAgent的代码，使用工具库。在Plugin中添加了方法sleePlgSIf
-----------------------------------------------------------------
版本		1, 0, 0, 3
变更说明	修改了CallAgent中的几个BUG，在发起呼叫成功后，立即清除原有消息。
		与服务器之间通过心跳保持连接。
		修改了在通道异常后没有回收通道的BUG
-----------------------------------------------------------------
版本		1, 0, 0, 4
变更说明	录音的超时时长改为一小时
		为了优化代码，去掉了Service的finalize方法
-----------------------------------------------------------------
版本		1, 0, 0, 5
变更说明	修改Mydate类中，compareTo方法参数对是否为null的判断。在vbs里，
		涉及到日期类型为空是无法比较的，目前版本中，日期类型和空比，返回值定为1。

		修改vbsparser里面ExprLogic类，增添多态的toStr方法，
		以解决msgbox的参数为一个逻辑表达式，编译通不过的bug.

----------------------------------------------------------------
版本		1, 0, 0, 6
变更说明	咨询转移返回NOCALL，始终失败的bug。

		修改com.channelsoft.slee.channelmanagerr里面ATSChannel类的consultCall方法，
		在调用makeCall前判断话路是否存在，并初始化makeCall的第一个参数portWrapper值为当前的端口号.
		修改routeConnection和unrouteConnection两个方法的输出，强制把其参数从double类型转换到int.
                 
------------------------------------------------------- 
版本		1, 0, 0, 7
变更说明	重构代码，把Util里可重用的类放到工具库中。
		修改sleeSession里取startParam参数最后一个始终为""的bug.
------------------------------------------------------------------------
版本		1, 0, 0, 8
变更说明	修改TTSFile里第三方放Float拼文件错误的bug;
		修改CallAgent里asr返回事件里没有返回识别结果的bug.
------------------------------------------------------------------------
版本		1, 0, 0, 9
变更说明	修改CallAgent的挂机方法，等待挂机事件到达后才算完成
------------------------------------------------------------------------
版本		1, 0, 0, 10
变更说明	修改vbs2java对sleeCallController.releaseConnection内置对象的翻译，无论有无参数，都翻译成无参数的                              sleeCallController.releaseConnection()形式.
		修改CallAgent里的translateMGReason方法，当MG事件是MGREASON_TM_USRSTOP时，返回成功ERR_Success.              
------------------------------------------------------------------------
版本		1, 0, 0, 11
变更说明	修订SNMP包中SnmpAgentHandler.sendTrap方法，在Trap消息头中增加对代理端IP值的设置。
		遵循SNMP协议修订trap报文消息头的specific trap,和generic code字段，
		以后对trap报文的扩展将通过扩展generic code字段来实现。
------------------------------------------------------------------------
版本		1, 0, 0, 12
变更说明	增加或修改与SLEEAdminAgent之间的网管消息包通信功能，支持ATSEAdmin监控
------------------------------------------------------------------------
版本		1, 0, 0, 13
变更说明	在SNMP.SnmpAgentHandler类中增加对4个get属性的处理,包括:startServiceCount,endServiceCount,
		successfulServiceCount,failedServiceCount,这4个统计数据由通道管理器ChannelManager.java维护.
------------------------------------------------------------------------
版本		1, 0, 0, 14
变更说明	重构了代码，把Mydate、Variant、TypeMismatchException、Utility挪到ReusableLib中。
		修改了SLEE启动时写日志的方式，不直接使用snmp的接口。
		增加vbs2java里对for语句循环变量类型为变体或循环终止值为变体的支持。
-----------------------------------------------------------------------------------------------------------
版本		1, 0, 0, 15
变更说明	放音、录音、放TTS 增加语音编码格式参数字段（6K or 8K）
		在MAKECALL时，号码中的逗号解释为停顿3秒，逗号后面的字符解释为SENDDTMF
		SendDTMF时，每送一个键，休眠500毫秒
		在放音时，如果从服务开始已经有指定的秒数内没有过与用户的过程，则提示是否继续。如果没有确认，则挂机。
		配置文件增加一个参数MaxUserIdleSeconds，配置允许用户无交互的秒数。
		支持PlayVoiceByTTS时选择六种语音库
		内置对象SLEECallController增加SetMaxCallTime方法，来实现对呼叫最长时间的限制，暂未实现，需要ICE接口配合。
		内置对象SLEECallController增加SingleStepHalfConnect方法，实现采用单工方式搭接另一个端口的功能，需要ICE接口配合。
		内置对象SLEECallController增加OtherPartyTrunkID的属性，表示三方会议中的第三方的端口号
		增加ASREdit2节点，是GetASR和VoiceListEdit的组合。以支持在ASR过程中的指定长度、指定打断、指定超时的收键动作，TTS/File列表放音，ASR语音识别功能。
		内置对象SLEECallController的SingleStepTransfer和SingleStepConference增加一个参数timeout，指定超时秒数
		内置对象SLEECallController的SingleStepConference增加一个参数routeFirst，指定是否提前搭接
		修改Util里，split方法当以分隔符结尾并且分隔符是特殊字符而出错的bug
		修改ReferenceNode里翻译输入知识变量前没加var_的bug.
--------------------------------------------------------------------------------------
版本		1, 0, 0, 16
变更说明	添加对com组件翻译的支持。
-----------------------------------------------------------------------------------------------------------
版本		1, 0, 0, 17
变更说明	添加对数据库连接池的支持，重构了配置文件的代码，使用JAXB，放弃使用DOM。
		支持 sleecallcontroller.routeConnection intPortNum, booleanIsHalf, booleanInverse
		sleecallcontroller.unrouteConnection intPortNum, booleanIsHalf, booleanInverse两个接口，允许业务选择通信方式（是否半工），选择搭接或拆除方向。
-----------------------------------------------------------------------------------------------------------
版本		1, 0, 0, 18
变更说明	重构了日志组件，使用ReusableLib中的日志组件。
		完成了对于自定义组件的支持
		实现了SLEE的ComContext
		实现对openjms，websphere MQ，点对点消息发布
-----------------------------------------------------------------------------------------------------------
版本		1, 0, 0, 19
变更说明	修改了CallAgent中没有限制重复发起外呼的BUG。
		完成了对ServiceProvider的支持。
		通过ReasoningServiceProvider提供了对USStudio的远程调试的支持。
		修改了SleeSession.msgbox()中对于字符串中有“%”时出错的BUG。
-----------------------------------------------------------------------------------------------------------
版本		1, 0, 0, 20
变更说明	在ICE版本的CallAgent中加入操作超时限制。
----------------------------------------------------------------------------------------
版本		1, 0, 0, 21
变更说明	修改sleeCallController.consult接口的timeout参数单位为秒。
		修改了PerfMonitor可能会退出线程的bug。
----------------------------------------------------------------------------------------
版本		1, 0, 0, 22
变更说明	修改了SOCKET版本中外呼失败后，通道无法继续外呼的BUG。
		增加了对新版本的监控MIB的支持。
		增加了和CCOD CMS对接的CallAgent相关代码。
----------------------------------------------------------------------------------------
版本		1, 0, 0, 23
变更说明	修改了ICE连接失败后没有释放连接，导致线程数上升的BUG。
----------------------------------------------------------------------------------------
版本		1, 0, 0, 24
变更说明	修改了对DLL的参数个数的支持，增加到20个。
----------------------------------------------------------------------------------------
版本		1, 0, 0, 25
变更说明	修改了PreCompile时不装载组件定义，导致编译失败的BUG。
----------------------------------------------------------------------------------------
版本		1, 0, 0, 26
变更说明	修改了ChannelManager和Channel的同步机制，避免有时发生的wait晚于notify的bug。
----------------------------------------------------------------------------------------
版本		1, 0, 0, 27
变更说明	增加VBs2java支持组件方法返回类型为自定义类功能。
		把ICE版本的CallAgent的超时时长改为15秒。
		在配置文件中，支持在组件注册中定义组件自己的配置信息。
		对com组件是变体类型的参数进行类型匹配与转换；
		修改sleeService.setAccountType方法的参数从double向int类型的强制转换；
		修改singleStepTransfer方法3个参数时，未进行类型匹配的问题；
		PreCompile工具支持在命令行指定Encoding，若未指定，则使用ISO8859-1
		修改词法分析过程中单独一个注释符号'而没有任何注释字符出错的bug
		支持sub关键字
		支持call关键字
		修改对singlesteptransfer和singlestepconference参数翻译错误的问题
		增加自定义函数内代码支持组件的功能；
		增加自定义函数可以在任意语句分支定义的功能；
		修改sleecallcontroller.consult方法参数类型转换的问题；
		添加了sleecallcontroller.closeconference接口
----------------------------------------------------------------------------------------
版本		1, 0, 0, 28
变更说明	增加了对短信IO节点的支持。修改了配置文件，增加了短信网关地址的配置。
----------------------------------------------------------------------------------------
版本		1, 0, 0, 29
变更说明	修改减法表达式没有变体到数字类型转换的错误
		修改对Sub关键字大小写敏感的bug
		修改vb2java的报错
----------------------------------------------------------------------------------------
版本		1, 0, 0, 30
变更说明	在放音的日志中，添加放音文件的名称。
		修改了调用DLL的功能中，对类型声明区分大小写的BUG。
----------------------------------------------------------------------------------------
版本		1, 0, 0, 31
变更说明	添加了与CCOD的GLS连接的代码。增加了配置文件的选项。
		取消了关于SNMP、SDR相关的环境变量，把它们配置在配置文件里。
----------------------------------------------------------------------------------------
版本		1, 0, 0, 32
变更说明	修改了在非SOCKET版本中，StopIO会等待不存在的ACK事件直至超时的BUG。
----------------------------------------------------------------------------------------
版本		2, 0, 0, 1
变更说明	建立2.0里程碑。
----------------------------------------------------------------------------------------
版本		2, 0, 0, 2
变更说明	添加了内置对象的方法sleecallcontroller.detachcall和attachcall。
		添加了内置对象的只读属性sleecallcontroller.otherpartycallsn。
		去掉了APP通道启动时，如果指定了启动文档则休眠一秒的功能。
		写话单出现异常时，休眠3秒。
		添加了内置对象的方法sleecallcontroller.disconnect，原有的disconnet是拼写错误，保留。
		修改了翻译代码中，不支持把sleeapp.getcontent()的返回值作为日期型值使用的BUG。
		修改了翻译代码中，自定义方法调用自定义方法时，产生的调用代码有两个下划线的BUG。
		修改了翻译代码中，sleeapp.getcontent等方法不支持参数为变体的BUG。
----------------------------------------------------------------------------------------
版本		2, 0, 0, 3
变更说明	修改了DLLCaller中，如果代码的最后是空行则会报错的BUG。
		USML文件路径中如果有非法字符，则生成类名时进行容错。
----------------------------------------------------------------------------------------
版本		2, 0, 0, 4
变更说明	修改了sleecallcontroller.singlestep翻译中，脚本的第4个参数没有转换成boolean型的BUG。
		不再主动计算通道占用率并告警，由管理控制台后台负责。
		新增了SNMP接口，支持从远程启动通道。
		修改了脚本中引用了不存在的内置对象的属性或方法时，会导致编译失败的BUG，现在会在代码翻译时报错。
		修改了不支持在脚本中使用加号连接两个字符串的BUG。
		修改了不支持脚本中对两种不同的并且不是变体的类型进行逻辑比较的BUG。
		修改了不支持脚本中的整除符号的BUG。
----------------------------------------------------------------------------------------
版本		2, 0, 0, 5
变更说明	如果配置中的SNMP发送地址配空，也不发告警信息。
----------------------------------------------------------------------------------------
版本		2, 0, 0, 6
变更说明	修改了代码中sleecallcontroller.answer接口的参数个数与文档不符的BUG，现在可以兼容0个和1个参数两种情况。
----------------------------------------------------------------------------------------
版本		2, 0, 0, 7
变更说明	在电话挂断后不把当前通道的curContact初始化，因为此变量中的主被叫在之后写话单时需要使用。
----------------------------------------------------------------------------------------
版本		2, 0, 0, 8
变更说明	写SDR时，如果时长不足一秒但大于等于500毫秒，则计为一秒。
		修改了代码翻译时不支持sleecallcontroller.callingnumber的BUG。
		在代码翻译时支持sleeservice.mappath。
----------------------------------------------------------------------------------------
版本		2, 0, 0, 9
变更说明	在翻译代码时，支持在组件的方法的参数中使用另一个组件或者自定义的类。
		在内置组件中添加了方法sleesession.vars和属性sleesession.varValue。
		在翻译代码时，允许组件的方法的参数是另一个组件或自定义的类。
----------------------------------------------------------------------------------------
版本		2, 0, 0, 10
变更说明	优化了ATSChannel的代码，提高性能。
		如果在配置文件中不配置SDR相关的属性和短信网关的地址，则装载sleejni库时如果出错则不报错，也不启动写话单的线程。
----------------------------------------------------------------------------------------
版本		2, 0, 0, 11
变更说明	翻译代码时，把变体类型的getString改成toString，防止在翻译组件类型时出错。
         增加Ice链路监控：
		SGEventReceiver通过IceConnectionMonitor监控链路。
		修改了当通道的入口文档不同但文件日期相同的情况下，会启动旧文档的BUG。
----------------------------------------------------------------------------------------
版本		2, 0, 0, 12
变更说明	翻译代码时，允许自定义的方法的声明中，不带左右括号。
		翻译代码时，支持以_充当连接符。
		翻译代码时，把err.number翻译为0，把err.description翻译为""。
		翻译代码时，自定义方法的"throws"与前面的括号间添加了空格。
----------------------------------------------------------------------------------------
版本		2, 0, 0, 13
变更说明	CallAgent增加接口attachCall。
		修改了翻译脚本时，遇到tab时会出错的BUG。
----------------------------------------------------------------------------------------
版本		2, 0, 0, 14
变更说明	在翻译代码时，不翻译注释。
----------------------------------------------------------------------------------------
版本		2, 0, 0, 15
变更说明	增加内置接口SLEEApp.StartServiceForOtherParty。
		解除了必须针对操作系统的字符集做单独编译的限制。
----------------------------------------------------------------------------------------
版本		2, 0, 0, 16
变更说明	修改了翻译代码时把以err开头的变量作为内置对象的BUG。
----------------------------------------------------------------------------------------
版本		2, 0, 0, 17
变更说明	修改了翻译代码时把err.description当作方法而不是属性的BUG。
		修改了翻译代码时，<>的一边是variant时，括号位置错误的BUG。
		修改了翻译代码时，数学运算符的结果不能转换为数字类型的BUG。
		修改了翻译代码时，节点的名称中有特殊字符就会出错的BUG。
		修改了翻译代码时，不支持日期型变量相减的BUG。
		在计算节点调用pluging时，如果参数没有初始值，则容错。
		修改了翻译代码时，翻译ObjectWrapper类型的参数时会发生错位的BUG。
		修改了翻译代码时，引用节点的名称中有回车符则可能出错的BUG。
		修改了翻译代码时，四则运算中的结果参与逻辑运算时，如果四则运算中的变量类型在之后会发生变化则会出错的BUG。
		修改了翻译代码时，引用节点的名称中的回车符会导致出错的BUG。
		修改了翻译代码时，在函数中使用数组型参数会编译失败的BUG。
----------------------------------------------------------------------------------------
版本		2, 0, 0, 18
变更说明	启动sleePlugin时，如果sleePlugin库不存在，忽略此错误。
----------------------------------------------------------------------------------------
版本		2, 0, 0, 19
变更说明	在CCOD版的callagent中，如果应答时回调CMS接口出现socket异常，则重置连接。
----------------------------------------------------------------------------------------
版本		2, 0, 0, 20
变更说明	翻译代码时，对SLEECallController.Answer接口参数进行翻译。socket版本支持该参数。
		从CCOD取随路数据时，进行中文代码转换。
		在CCOD版的callagent中，清除会话的操作延迟到分发消息时进行。
----------------------------------------------------------------------------------------
版本		2, 0, 0, 21
变更说明	在Socket版的callagent中，发送文本时强制使用GBK编码。
----------------------------------------------------------------------------------------
版本		2, 0, 0, 22
变更说明	在CCOD版的callagent中，连接DDS。
		配置文件中，CCOD节增加配置项，指定统计文件的生成路径。
		在取得CCOD的随路数据时，自动获得呼叫的SessionID和EnterpriseID。
		在保存CCOD的随路数据时，自动传递呼叫到达时间和接通时间。
----------------------------------------------------------------------------------------
版本		2, 0, 0, 23
变更说明	按<<日志格式设计规范.doc>>，规范日志。
----------------------------------------------------------------------------------------
版本		2, 0, 0, 24
变更说明	在CCOD版的callagent中，添加了写话单和填充随路数据的功能。
		在配置文件中添加了指定CCOD固定的ServiceID的项。
----------------------------------------------------------------------------------------
版本		2, 0, 0, 25
变更说明	在Socket版的callagent中，发送文本时使用缺省编码。
----------------------------------------------------------------------------------------
版本		2, 0, 0, 26
变更说明	在CCOD版的callagent中，写话单时去掉电话号码冒号前面的部分，主被叫位置互换。
		在CCOD版的callagent中，CMS注册回调接口时，记住CMS服务的名称。不支持多个CMS同时连接的情况。
		在CCOD版的callagent中，随路数据中添加了CMSNAME等内容。
		在CCOD版的callagent中，写DDS话单时，保证至少最长间隔时间后会出一个话单文件。
		在CCOD版的callagent中，写DDS话单时，旧电话不再产生假接通事件。
		修改了取内置对象的SLEECallController.ChannelDn时返回Port的BUG。
		支持ActiveMQ
		不再支持OpenJMS
----------------------------------------------------------------------------------------
版本		2, 0, 0, 27
变更说明	业务接口SingleStepTransfer支持通话模式和搭接顺序设置
		支持SleePlg.sif操作
----------------------------------------------------------------------------------------
版本		2, 0, 0, 28
变更说明	在拼接语音文件的路径时，如果文件名以"/"开头，则认为是UNIX格式下的绝对路径，只转换分隔符。
		同时在放音和录音时，UNIX的路径只以相对路径拼接。
----------------------------------------------------------------------------------------
版本		2, 0, 0, 29
变更说明	在CCOD版的callagent中，写话单时以企业ID作为subscriber_id。
----------------------------------------------------------------------------------------
版本		2, 0, 0, 30
变更说明	修改命令行启动方式，直接以java -jar方式启动, 自动搜索并更新类路径。
		支持命令行参数-help，只打印命令行使用说明。
		支持命令行参数-version，只打印版本信息。
		支持命令行参数-releasenote，只打印此文件的内容。
		在启动时自动搜索jar文件并添加了到类路径中。
		为每个线程设置名字，以便于以后调试。
		在CCOD版的callagent中，应答失败时，给监控发挂机事件。
----------------------------------------------------------------------------------------
版本		2, 0, 0, 31
变更说明	在CCOD版的callagent中，写统计数据时只判断统计数据队列是否满，监控数据队列满也不影响写统计数据。
		在CCOD版的callagent中，启动服务时在日志中打印主被叫号码。
		在CCOD版的callagent中，转移服务时的假挂机事件的时间戳取shiftservice的输出参数中的值。
		实现了SLEEComponentContext中的接口getComponentRegistryByProgID。
		
		重定义服务统计数据：
		启动服务总数: 触发占用服务通道总数
		结束服务总数: 退出占用服务通道总数
		服务成功总数: 进入业务推理操作总数
		服务失败总数: 加载业务流程失败、应答失败总数
----------------------------------------------------------------------------------------
版本		2, 0, 0, 32
变更说明	在CCOD版的callagent中，转移服务时的假挂机事件的时间戳取shiftservice的输出参数中的值。
----------------------------------------------------------------------------------------
版本		2, 0, 0, 33
变更说明	在CCOD版的callagent中，如果GLS服务器的地址配置为0.0.0.0，则不连接GLS。
		在CCOD版的callagent中，支持SingleStepTransfer。
		在外呼时网关返回资源不足时，返回EVENT_InsufficientResource。
		配置文件增加一个参数SupportFileOnUnix，配置是否支持UNIX格式的放音文件路径，默认为true。
		修改了内置方法MsgBox的中，会把%号输出两遍的BUG。
----------------------------------------------------------------------------------------
版本		2, 1, 0, 1
变更说明	重写了VBS翻译功能，支持完整的VBS规范。
		配置文件增加一个参数SupportFileOnUnix，配置是否支持UNIX格式的放音文件路径。
		在CCOD和ICE版的callagent中，atsSendSignals支持按键逐个发送，键间延时500ms。
----------------------------------------------------------------------------------------
版本		2, 1, 1, 0
变更说明	在CCOD版的callagent中，支持新的DDS接口。
		在CCOD版的callagent中，修改了把某些话路不存在的错误识别为ERR_GeneralError的BUG。
----------------------------------------------------------------------------------------
版本		2, 1, 2, 0
变更说明	重构SysCfgData类，对外提供统一的配置接口。解除其它模块与SysCfgData类之间的耦合，便于提供另外的配置实现方式。
		修改了内置方法filter解释第三个参数有错误的BUG。
		修改了vbs翻译错误:a=fun(a,...),将第二个a当成自定义函数。
		vbs支持private const ....语句。
		vbs支持is操作符。
		修改了内置方法FormatCurrency、FormatNumber、FormatPercent中的BUG。
		在IO节点出现短信相关的错误时，输出的m_PrevResult值为SmsError而不是输出GeneralError。
		DDSClientDll初始化路径改为配置文件<CCOD StatFileDir.../>所配置的路径。
----------------------------------------------------------------------------------------
版本		2, 1, 2, 1
变更说明	修改了在Socket和ICE版本的CallAgent中，外呼时如果主叫挂机，则在挂断外呼通道时更新代表外呼
		通道的CallAgent的状态时，没检查此CallAgent是否已经被新的外呼所占用，从而导致新的外呼成功后
		CallAgent的状态一直是Idle的BUG。
		修改了在CCOD的CallAgent中，在使用DDSClient库连接DDS不成功时没有休眠就直接重连的BUG。
----------------------------------------------------------------------------------------
版本		2, 1, 3, 0
变更说明	SLEE在启动时如果出现错误退出时，在屏幕上打印出错原因。
		重构了VBS翻译中，装载内置对象的代码。以后新增内置对象只需要修改ParserWord.loadInternalObj就可以了。
		修改了内置方法中的一些对于把特殊数据转换成日期时与VBS规范不符的BUG。
		在VBS翻译中，在更新输出变量的代码之前，把当前行号设置为0。
----------------------------------------------------------------------------------------
版本		2, 1, 3, 1
		修改了在ICE版的CallAgent中，放音会直接返回ERR_RPC的BUG。
----------------------------------------------------------------------------------------
版本		2, 1, 3, 2
		修改了VBS翻译中，把int函数直接当作cint函数处理的BUG。
----------------------------------------------------------------------------------------
版本		2, 1, 4, 0
变更说明	支持内置方法GetObject，仅支持在脚本中取系统当前IP地址的代码。
		修改了在VBS翻译中，会忽略在createobject后的代码的BUG。
		修改了在VBS翻译中，对for each的翻译会丢失数组的最后一个元素的BUG。
		对于JniFunction，只保留Plugin相关的方法，写话单、调用DLL均改用JNA实现（不支持AIX）。
----------------------------------------------------------------------------------------
版本		2, 1, 4, 1
变更说明	在CCOD版本的CallAgent中，三方通话时，如果外呼方挂断，则对主叫方的媒体连接进行接回。
----------------------------------------------------------------------------------------
版本		2, 1, 4, 2
变更说明	修改了在CCOD版本的CallAgent中，写外呼SDR时原始主被叫填为null的BUG。
----------------------------------------------------------------------------------------
版本		2, 1, 4, 3
变更说明	修改bug：vbs的方法缺省参数应该翻译为null。
----------------------------------------------------------------------------------------
版本		2, 1, 4, 4
变更说明	修改了在CCOD版本的CallAgent中，异步放音放第二遍时会失败的BUG。
----------------------------------------------------------------------------------------
版本		2, 1, 4, 5
变更说明	修改了在SDR中，保留字段等会被填为null的BUG。
----------------------------------------------------------------------------------------
版本		2, 1, 5, 0
变更说明	添加了WriteDTMFLog的参数，指定是否在日志中记录收DTMF的结果。默认为true。
----------------------------------------------------------------------------------------
版本		2, 1, 6, 0
变更说明	在CCOD版本的CallAgent中，支持使用SSL。
		在配置文件中的CCOD小节中添加了UseSSL，指示是否使用SSL连接CMS，并且使用Config目录下的
		Ice_SLEE4CCODCMSServer.cfg作为ICE的配置文件。默认为false。
----------------------------------------------------------------------------------------
版本		2, 1, 6, 1
变更说明	修改了在CCOD版本的CallAgent中，不应答就放音会找不到MepId的BUG。
----------------------------------------------------------------------------------------
版本		3, 0, 0, 0
变更说明	引用节点添加Goto属性。仅对跨文档引用有效。表示在调用子文档完毕后，是否继续执行引用节点之后的节点。
		添加ExitWorkflow节点。停止执行当前工作流。
		添加Sleep节点。在有电话的状态时阻塞当前通道至SLEEP结束；
			无通话的状态时释放当前通道，SLEEP结束后使用空闲的APP通道继续执行流程。
		添加SendMessage节点。向消息队列发消息。可以指定会话ID。
			可以指定目标消息队列，默认为SLEE内部队列，此时必须指定会话ID。
			不允许向不存在的会话发消息。可以指定消息的有效期。如果不指定则取系统默认的有效期。
			可以指定是否为持久化消息。
			如果是发送内部消息，则指定的有效期和持久化无效，内部消息都是非持久化的，并且不会超时。
		添加WaitMessage节点。从指定的消息队列中取消息。必须指定会话ID。
			可以指定目标消息队列，默认为SLEE内部队列，此时会话ID不需要指定，强制为当前会话ID。
			可以指定超时。等待消息时当前通道会被释放，消息到达或者超时后，使用空闲的APP通道继续执行流程。
		添加了配置参数MessagingService，支持指定默认的消息有效期、内部队列名称、会话有效期。

		监控功能待添加：
			当前挂起的会话数。
			查看所有挂起的会话。
			取消指定的会话。
			强制指定的会话超时。
			给指定的会话发消息。

		修改bug，将sysWavPath改为"D:\\ChannelSoft\\CsCCP\\SoftSwitch\\SysWav\\"。
		重构了CallAgent，合并重复的代码。
		修改了VBS翻译中，split方法不支持非字符串类型的变量的BUG。
		支持SSR2。
		支持视频呼叫。
		发短信时，字符编码固定写成15，防止流程出错导致乱码。

----------------------------------------------------------------------------------------
版本		3, 0, 1, 0
变更说明	添加监控功能：
			查看当前挂起的会话数。
			查看所有挂起的会话。
			取消指定的会话。
			强制指定的会话超时。
			给指定的会话发消息。
		修改bug，视频单步转移判断媒体类型时this.isVideoCall改为agent.isVedioCall.

----------------------------------------------------------------------------------------
版本		3, 0, 1, 1
变更说明	修改bug，ice版singlestepconference，要调用blindcreateconference（原来没调）。
		修改bug，singlestepconference只有两个参数时，翻译为singlestepconference(原来为singlesteptransfer)。
		修改bug，singlestepconference2只有两个参数时，翻译为singlestepconference（同上）。
		修改了在socket版的CallAgent中，放音会直接返回ERR_RPC的BUG。

----------------------------------------------------------------------------------------
版本		3, 0, 1, 2
变更说明	修改bug，App通道启动时设置sessionId。	
		修改了挂起并等待消息超时后，节点的返回事件为NoError的BUG，应该为Timeout。
		修改了挂起并接收到消息后，节点的消息内容没有被填充的BUG。
		在SSR2版本的CallAgent中，支持在单步转移时指定的媒体类型与入呼叫不符。
		修改了在CallAgent中，指定反向搭接无效的BUG。
		在所有版本的CallAgent中，SendDTMF的时候，不再拆成单个键发送。
		内置对象CallController添加了方法AttachOtherParty。
		修改了ICE版本的CallAgent中，收发传真时文件名没有转码导致乱码的BUG。
		写CCOD话单时，原始主被叫去掉'SIP:'、'TEL:'前缀；
----------------------------------------------------------------------------------------
版本		3, 1, 0, 0
变更说明	根据ATS4的规范，根据SLEE的新目录结构进行了相应的修改。	
		修改了在CCOD版本的CallAgent中，不应答就放音会找不到MepId的BUG。

----------------------------------------------------------------------------------------
版本		3, 1, 1, 0
变更说明	linux下使用的计费配置文件与windows一致：/ATS4/config/drwr_client.cfg。
		修改了SNMP统计中，启动服务次数错误计算了外拨通道的数据的BUG。
		修改了SNMP的Trap消息只发送一次的BUG。
----------------------------------------------------------------------------------------
版本		3, 1, 1, 1
变更说明	修改了CallAgent中，SendDTMF总是返回失败的BUG。
		支持旧的SoftSwitch目录结构和新的ATS4目录结构。
----------------------------------------------------------------------------------------
版本		3, 1, 1, 2
变更说明	修改了CallAgent中，关闭会议总是会不进行任何的BUG。
		上传服务的监听端口可以配置，添加了配置项ManageServerPort，默认为8889。
----------------------------------------------------------------------------------------
版本		3, 1, 1, 3
变更说明	修改了Bug。在ICE版中，发起BlindMakeCall选线成功后，要清除以前事件。
----------------------------------------------------------------------------------------
版本		3, 1, 2, 0
变更说明	通道管理器在分配入呼叫通道失败时，自动挂断入呼叫。
----------------------------------------------------------------------------------------
版本		3, 1, 2, 1
变更说明	修改了在自动外拨通道中，会话ID不连续的BUG。
		支持连接多CMS。
		支持CombineVoiceEdit，用于VoiceEdit和VoiceListEdit节点。
----------------------------------------------------------------------------------------
版本		3, 1, 2, 2
变更说明 收键超时时，要将已收到的按键返回给流程。
		外呼SDR的接入时间参数应为外呼接通时间。
		修改bug：CCOD中播放内置TTS时文件路径前应加上file:///。
		修改问题：事件通道中因处理挂断外呼通道导致通道堵塞，现改成通过线程池挂断外呼通道。
----------------------------------------------------------------------------------------
版本		3, 1, 3, 0
变更说明 支持CallEventListener类型的ServiceProvider。
----------------------------------------------------------------------------------------
版本		3, 1, 4, 0
变更说明  支持CCOD外呼业务，支持两种话单ivr和agent(与外线接通话记录)。
		CallAgent支持UCCE2，在配置文件里添加了有关UCCE2的配置项。
		修改bug：ccod中，呼外线时，主叫的accessTime变为0的bug。
----------------------------------------------------------------------------------------
版本		3, 1, 5, 0
变更说明  支持CCOD视频呼叫。<CCOD>增加配置项supportVideo="bool"	,表示是否支持视频。
		修改bug: 应答失败时，发送DDS事件中没有企业id。
		支持BossAgent。
		CCOD转外线时，增加支持并线录音操作。
		修改bug:CCOD版单步转移呼外线时，callId生成没有同步。
		CCOD支持unrouteConnection。
		修改bug:asr返回中文乱码。
		更新cms接口，支持新老cms。
		CCOD支持外呼。
----------------------------------------------------------------------------------------
版本		3, 1, 5, 1
变更说明  CCOD转外线并线录音时,增加原始被叫参数(被叫_record_原始被叫).
		CCOD转外线并线录音时,外线话单的被叫字段中去掉原始被叫.
		CCOD纯外呼,增加accountId参数，支持ccod计费.
----------------------------------------------------------------------------------------
版本		3, 1, 6, 0
变更说明  SSR2支持ivr外呼录彩铃音(CallRecordRing)操作。		
		ATS4、CCOD版本，makecall和singlesteptransfer时，支持原始主被叫。